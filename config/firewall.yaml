---
# By default egress traffic is not allowed, define below the ports allowed.
egress:
  - ports:
      - protocol: TCP
        port: 443
      - protocol: TCP
        port: 80
#ingress:
# For Cockpit
#  - ports:
#      - protocol: TCP
#        port: 9090

# For Apache webserver
  - ports:
      - protocol: TCP
        port: 80
